<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.android.training.valesalmacenj16">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_attempt"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_attempt_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ValesAlmacenJ16">
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SearchMedicineList"
            android:exported="false" />
        <activity android:name=".MainActivity"></activity>
    </application>

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
</manifest>